interface Resources {
  common: {
    lang: string;
  };
  projects: {
    Des: {
      eshop: string;
      todos: string;
      ecomerce: string;
      games: string;
      note: string;
    };
  };
}

export default Resources;
